#include <WiFi.h>  // Necesaria para obtener MAC

void setup() {
  Serial.begin(115200);
  delay(1000);

  // Mostrar dirección MAC
  Serial.println("Direccion MAC del ESP32:");
  Serial.println(WiFi.macAddress());
}

void loop() {
  // Nada en el loop
}
